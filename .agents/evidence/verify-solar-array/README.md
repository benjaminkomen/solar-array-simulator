# verify-solar-array evidence

Proof artifacts from `.cursor/skills/verify-solar-array` land here: JSON receipts from `doctor`/`smoke`/`run-flow`/`screenshot`, plus screenshots when a device backend can take one.

- **Keep this README.** It is the only tracked file in this directory.
- Receipts and images are gitignored. Do not commit them.
- `control.mjs cleanup` must **not** delete this directory or its artifacts.
- A missing screenshot on a machine without a simulator is not a failed skill; it is a missing device. Say so in the receipt.
